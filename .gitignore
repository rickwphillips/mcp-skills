node_modules/
dist/
*.log
.DS_Store
.env
.env.local
connections.json
.audit/
*.tsbuildinfo

# emitted by scripts/build-apps.mjs (npm run build:apps)
src/generated/
coverage/

# MissionControl injects .claude/skills/diagram + settings.local.json hooks on every session spawn
.claude/

# local runtime data (sqlite db)
/data/

# machine-local agent config
.opencode/
