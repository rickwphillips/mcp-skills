{
  "name": "mcp-skills",
  "version": "2.0.2",
  "description": "Agent-agnostic MCP server exposing reusable database, PDF, and workflow skills as Tools and Resources.",
  "license": "MIT",
  "author": "Rick Phillips",
  "repository": {
    "type": "git",
    "url": "https://github.com/rickwphillips/mcp-skills.git"
  },
  "type": "module",
  "bin": {
    "mcp-skills": "dist/server.js"
  },
  "files": [
    "dist",
    "README.md",
    "CHANGELOG.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "npm run build:apps && tsc",
    "build:apps": "node scripts/build-apps.mjs",
    "dev:apps": "node scripts/build-apps.mjs --watch",
    "start": "node dist/server.js",
    "dev": "tsc --watch",
    "typecheck": "npm run build:apps && tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4",
    "mysql2": "^3.11.5",
    "pdf-lib": "^1.17.1",
    "pdfjs-dist": "^4.10.38",
    "zod": "^3.24.1",
    "zod-to-json-schema": "^3.24.1"
  },
  "devDependencies": {
    "@modelcontextprotocol/ext-apps": "^1.7.5",
    "@types/node": "^22.10.2",
    "@vitest/coverage-v8": "^4.1.6",
    "esbuild": "^0.28.1",
    "preact": "^10.29.7",
    "typescript": "^5.7.2",
    "vitest": "^4.1.6"
  },
  "engines": {
    "node": ">=20"
  }
}
